## LocalRoute v0.1.1-beta

### 新增与修复

- 新增 macOS 状态栏菜单和 Windows 系统托盘，支持查看状态、启停代理、打开窗口和退出应用。
- 系统睡眠或休眠恢复后清理旧连接，保留监听端口和路由配置；已主动停止的代理不会自动启动。
- macOS 保留正常运行的端口桥接进程，减少重复授权；桥接进程退出后尝试重新启动。
- 状态栏启停增加操作中提示和重复点击保护，macOS 管理员授权增加激活处理与超时提示。
- Windows 资源管理器重启后恢复托盘图标。
- 修正监听端口绑定失败却显示运行中的问题。

### 下载与升级

- macOS：下载 `LocalRoute-macOS.zip`，解压后打开 `LocalRoute.app`。
- Windows x64：下载 `LocalRoute-Windows-x64.zip`，解压后运行 `LocalRoute.exe`。
- 每个压缩包提供对应的 `.sha256` 校验文件。
- 升级前请从旧版应用中选择“退出 LocalRoute”；仅关闭窗口会继续在后台运行。现有 YAML 配置可继续使用。

### 测试版说明

安装包尚未使用商业开发者证书签名或完成 Apple 公证，首次运行时系统可能显示安全提示。睡眠前进行中的请求或长连接可能断开，需由客户端重试或重连。真实睡眠/休眠及不同系统环境仍需实机验证，Windows 验证步骤见仓库的 `docs/windows-validation.md`。
